# Option 3 (SENTINEL_MONITOR=docker). Sentinel talks to the Engine API on
# /var/run/docker.sock directly; the socket is root-owned, so the capability-
# less root of the base unit can use it without extra privileges.
[Unit]
After=docker.service
